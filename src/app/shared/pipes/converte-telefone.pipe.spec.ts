import { ConverteTelefonePipe } from './converte-telefone.pipe';

describe('ConverteTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new ConverteTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
